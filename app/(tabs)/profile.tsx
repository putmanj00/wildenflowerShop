/**
 * Wildenflower — Profile Screen
 * See CLAUDE.md for full spec.
 */
import Screen from '../../components/layout/Screen';

export default function ProfileScreen() {
  return <Screen>{/* TODO: Build — see CLAUDE.md */}</Screen>;
}
