---
phase: 11
plan: 1
wave: 1
---

# Plan 11.1: PrimaryButton Wreath Variant + HeroCard Integration

## Objective
Wire the unused `button-wreath.png` asset into PrimaryButton as a new wreath variant, then update HeroCard to use it for its "Wander the Shop" CTA — completing the botanical thematic coverage of CTA buttons.

## Context
- .planning/ROADMAP.md — Phase 11 definition
- components/PrimaryButton.tsx — Current button (gold/terracotta variants only)
- components/HeroCard.tsx — Uses PrimaryButton with variant="gold"
- assets/images/icons/ui/button-wreath.png — Existing unused asset (480×168)

## Tasks

<task type="auto">
  <name>Add wreath variant to PrimaryButton</name>
  <files>components/PrimaryButton.tsx</files>
  <action>
    1. Import Image from react-native and require `button-wreath.png`
    2. Add "wreath" to the variant union type: `'gold' | 'terracotta' | 'wreath'`
    3. Create a wreath variant style with transparent background
    4. Wrap button content in a View container with the wreath image as an absolutely-positioned background Image (resizeMode="contain")
    5. Label text for wreath variant should use colors.earth (same as gold)
    6. Keep existing gold and terracotta render paths UNCHANGED
    - DO NOT change the component's external API beyond adding the new variant option
    - DO NOT change the existing gold/terracotta styles
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep PrimaryButton</verify>
  <done>PrimaryButton renders wreath variant with button-wreath.png background; gold and terracotta variants unchanged</done>
</task>

<task type="auto">
  <name>Update HeroCard CTA to use wreath variant</name>
  <files>components/HeroCard.tsx</files>
  <action>
    1. Change HeroCard's PrimaryButton from variant="gold" to variant="wreath"
    - That's it — single prop change
    - DO NOT change any other part of HeroCard
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep HeroCard</verify>
  <done>HeroCard "Wander the Shop" button renders with wreath background image</done>
</task>

## Success Criteria
- [ ] PrimaryButton with variant="wreath" renders button-wreath.png behind the label
- [ ] HeroCard CTA shows wreath decoration
- [ ] PrimaryButton with variant="gold" and variant="terracotta" render identically to before
- [ ] No TypeScript errors
